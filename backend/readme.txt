/**
 * Rota / resurso
*/
/**
  * Métodos HTTP:
  * GET: Buscar/listar uma informação no back-end
  * POST: Criar uma informação no back-end
  * PUT: Alterar informação no back-end
  * DELETE:Deletar informação no back-end
*/

/**
 * Tipos de parâmetros:
 * 
 * Query Params: Parâmetros nomeados eviados na rota após  "?" (Filtros,Paginação)
 * Route Params: Parâmetros utilizados para identificar recursos
 * Request Body: Corpo da requisição,utilizado para cirar ou alterar recursos
*/

/**
 * SQL: Mysql, SQLite, PostgreDWL, Oracle,Microoft SQL Server
 * NoSQL: MongoDB, CouchB, etc
 */

 /**
  * Driver: SELECT * FROM users
  * Query Builder: table('users').selecr('*').where()
  */